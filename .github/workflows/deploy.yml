name: Deploy

concurrency: deploy

permissions:
  id-token: write
  contents: read


on:
  push:
    branches:
      - main

jobs:
  test:
    uses: ./.github/workflows/test.yml

  # build-and-push-to-ecr:
  #   name: Build and push to ECR
  #   runs-on: blacksmith-2vcpu-ubuntu-2404-arm
  #   needs: [test]
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Configure AWS credentials
  #       uses: aws-actions/configure-aws-credentials@v4
  #       with:
  #         role-to-assume: arn:aws:iam::787309967787:role/atlas-axis-github-iam-role
  #         aws-region: eu-west-1

  #     - name: Login to Amazon ECR
  #       id: login-ecr
  #       uses: aws-actions/amazon-ecr-login@v2

  #     - name: Build, tag, and push docker image to Amazon ECR
  #       env:
  #         REGISTRY: ${{ steps.login-ecr.outputs.registry }}
  #         REPOSITORY: atlas-axis 
  #         IMAGE_TAG: ${{ github.sha }}
  #       run: |
  #         docker build -t $REGISTRY/$REPOSITORY:$IMAGE_TAG .
  #         docker push $REGISTRY/$REPOSITORY:$IMAGE_TAG

  # deploy:
  #   needs: [build-and-push-to-ecr]
  #   runs-on: blacksmith-2vcpu-ubuntu-2404-arm
  #   steps:
  #     - name: Set up Python 3.x
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: 3.13

  #     - name: Install dependencies
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install ecs-deploy==1.15.2

  #     - name: Configure AWS credentials
  #       uses: aws-actions/configure-aws-credentials@v4
  #       with:
  #         role-to-assume: arn:aws:iam::787309967787:role/atlas-axis-github-iam-role
  #         aws-region: eu-west-1

  #     - name: Deploy new arq version
  #       run: ecs deploy ba-cluster atlas-axis-arq --tag ${{ github.sha }} --timeout -1
  
